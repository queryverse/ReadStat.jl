language: julia
os:
  - linux
  - osx
julia:
  - 0.7
  - nightly
notifications:
  email: false
branches:
  only:
  - master
  - /release-.*/
  - /v(\d+)\.(\d+)\.(\d+)/
after_success:
  - julia -e 'cd(Pkg.dir("ReadStat")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(process_folder())'
